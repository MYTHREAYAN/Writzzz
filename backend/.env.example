# Server
NODE_ENV=development
PORT=5000

# MongoDB Atlas
MONGODB_URI=mongodb+srv://writzzzadmin:Writzzz%402026%40DB@cluster0.oznb4or.mongodb.net/trio_assignment?retryWrites=true&w=majority&appName=Cluster0retryWrites=true&w=majority&appName=Cluster0

# JWT
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=7d

# HTTP-only auth cookie
COOKIE_NAME=trio_auth_token
COOKIE_MAX_AGE_MS=604800000

# Frontend origin
CLIENT_URL=http://localhost:5173

# Password reset token lifetime
RESET_TOKEN_EXPIRY_MINUTES=30

# Optional SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="TRIO ASSIGNMENT noreply@trioassignment.local"